[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
markers =
    integration: Integration tests that require RefNet service to be running
    unit: Unit tests that don't require external services
    slow: Slow tests that take more than 5 seconds
